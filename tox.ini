[tox]
envlist = py27, py36

[testenv]
commands = pytest --cov=ladybug tests/
deps =
    pytest
    pytest-cov
    git+https://github.com/ladybug-tools/ladybug-geometry.git@development
[flake8]
max-line-length = 89
